runtime: nodejs20 # or another supported version

instance_class: F1

env_variables:
  SMTP_USER: $SMTP_USER
  SMTP_PWD: $SMTP_PWD


handlers:
  - url: /static
    static_dir: /client

# handlers:
# - url: /stylesheets
#   static_dir: stylesheets

# - url: /.*
#   secure: always
#   redirect_http_response_code: 301
#   script: auto